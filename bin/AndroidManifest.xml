<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cybertrons.orfa"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation = "landscape"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/menu_welcome" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartStoryAvtivity" />
    	<activity android:name=".EditStudentActivity" />
    	<activity android:name=".ImportStoryActivity" />
    	<activity android:name=".AssignSoundsActivity" />
    	<activity android:name=".MarkWordActivity" />
        <activity android:name=".DisplayButtonsActivity" />
    	<activity android:name=".DbTesterActivity" />
    </application>

</manifest>